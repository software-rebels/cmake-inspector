option(BUILD_CURL	"Build the CURL library" ON)
add_executable(exec foo.cpp)
add_library(openssl openssl.h)
add_library(curl curl.h)
target_link_libraries(exec openssl)
target_link_libraries(openssl curl)
if(BUILD_CURL)
    target_link_libraries(curl openssl)
endif()